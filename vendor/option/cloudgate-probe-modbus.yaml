name: cloudgate-probe-lora-modbus
description: Compact DIN rail mountable metering device that can connect devices over Modbus (RS-485), Digital & Analog Inputs to a LoRa network.

# Hardware versions (optional)
hardwareVersions:
  - version: '5.0'
    numeric: 1

# Firmware versions (at least one is mandatory)
firmwareVersions:
  - # Firmware version
    version: '2.0.6'
    numeric: 1
    # Supported hardware versions (optional)
    hardwareVersions:
      - '5.0' # Must refer to hardwareVersions declared above
    # LoRaWAN Device Profiles per region
    # Supported regions: EU863-870, US902-928, AU915-928, AS923, CN779-787, EU433, CN470-510, KR920-923, IN865-867, RU864-870
    profiles:
      EU863-870:
        id: cloudgate-probe-modbus-profile-eu868
        lorawanCertified: false
        codec: cloudgate-probe-modbus-codec
      US902-928:
        id: cloudgate-probe-modbus-profile-us915
        lorawanCertified: false
        codec: cloudgate-probe-modbus-codec

weight: 74
ipCode: IP20

photos:
  main: cloudgate-probe.png

operatingConditions:
  temperature:
    min: -20
    max: 60
  relativeHumidity:
    min: 0.05
    max: 0.95
